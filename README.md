# My blog!
---
I always thought these were cool so I figured I would try my hand at one.
This one's based on hugo and papermod. Nothing fancy whatsoever.

```
cd blog && hugo && rsync -avz --delete public/ blog_server:~/www/
```
